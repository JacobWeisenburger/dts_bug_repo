{
    "name": "dts_bug_repo",
    "module": "index.ts",
    "type": "module",
    "scripts": {
        "build": "clear && bun --watch run scripts/build.ts"
    },
    "devDependencies": {
        "@types/bun": "latest",
        "bun-plugin-dts": "^0.2.1"
    }
}